apiVersion: v1
kind: Service
metadata:
  name: {{ include "movie-watchlist.fullname" . }}
  labels:
    {{- include "movie-watchlist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movie-watchlist.selectorLabels" . | nindent 4 }}